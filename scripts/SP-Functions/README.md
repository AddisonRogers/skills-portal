# sp-functions

This is a basic set of scripts to allow for updating and modification of the developer-roadmap repo to a blob and postgres store.

```bash
bun install
```

To run:

```bash
bun run index.ts
```